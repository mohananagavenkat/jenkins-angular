import { ShowMoreDirective } from './show-more.directive';

describe('ReadMoreDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowMoreDirective();
    expect(directive).toBeTruthy();
  });
});
